Github Session:

Issues:
    Data Transfer.
    Accountablity.
    History of my code.

Solution:
    GIT -> GITHUB supports GIT
    Multiple platform for GIT like:  GITHUB , GITLAB, GITBUCKET

    GITSCM to use GIT local

Repository (Folder)

Hammas -> Code -> Github
Github -> Code -> Hammas2

Github Repo Create:
    local Computer -> cmd / vscode terminal

    git init 
    git add . -> add all the files

    git config --user.name ""
    git config --user.emai ""
    git commit -m "message"

    git branch -M main
    git remote add origin "URL"

    git push -u origin main

git log 
    git reset --hard (commit id)
